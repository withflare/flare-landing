# Postgres connection string — required for the waitlist API (see app/api/waitlist/route.ts)
# Copy this file to .env.local and set DATABASE_URL to your Neon connection string.
# Do not put real credentials in .env.example.
DATABASE_URL="postgresql://USER:PASSWORD@YOUR-POOLER-HOST.neon.tech/neondb?sslmode=require"
